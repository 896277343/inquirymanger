import { requireLeader } from "@/lib/auth";import { rows } from "@/lib/db";
export default async function Logs(){await requireLeader();const logs=rows<any>("SELECT l.*,u.display_name FROM audit_logs l JOIN users u ON u.id=l.user_id ORDER BY l.created_at DESC LIMIT 300");return <><div className="topline"><div><div className="title">操作日志</div><p className="subtle">记录分配、客户修改、询盘状态和跟进行为</p></div></div><section className="panel"><table className="table"><thead><tr><th>时间</th><th>操作人</th><th>动作</th><th>对象</th><th>说明</th></tr></thead><tbody>{logs.map(l=><tr key={l.id}><td>{l.created_at}</td><td>{l.display_name}</td><td>{l.action}</td><td>{l.entity_type} #{l.entity_id||"—"}</td><td>{l.summary}</td></tr>)}</tbody></table>{logs.length===0&&<p className="subtle">暂无操作记录。</p>}</section></>}
