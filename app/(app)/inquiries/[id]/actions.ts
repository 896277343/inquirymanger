"use server";import { revalidatePath } from "next/cache";import { requireSession,requireLeader } from "@/lib/auth";import { audit,db,row } from "@/lib/db";import { decryptSecret } from "@/lib/secrets";import { syncToWukong } from "@/lib/wukong";
async function canAccess(id:number){const s=await requireSession();const q=row<any>("SELECT owner_id FROM inquiries WHERE id=?",id);if(!q||(s.role==="SALES"&&q.owner_id!==s.id))throw new Error("无权访问");return s}
export async function assignInquiry(formData:FormData){const s=await requireLeader(),id=Number(formData.get("id")),owner=Number(formData.get("owner"));const u=row<any>("SELECT display_name FROM users WHERE id=? AND role='SALES' AND active=1",owner);if(!u)throw new Error("业务员不存在");db.prepare("UPDATE inquiries SET owner_id=?,status='NEW',assigned_at=datetime('now','localtime'),updated_at=datetime('now','localtime') WHERE id=?").run(owner,id);audit(s.id,"ASSIGN","INQUIRY",id,`分配给 ${u.display_name}`);revalidatePath(`/inquiries/${id}`);revalidatePath("/dashboard")}
export async function updateInquiryDetails(formData:FormData){const s=await requireLeader(),id=Number(formData.get("id"));const current=row<any>("SELECT i.customer_id,i.source,i.product_category,i.requirement,c.primary_email FROM inquiries i JOIN customers c ON c.id=i.customer_id WHERE i.id=?",id);if(!current)throw new Error("询盘不存在");const email=String(formData.get("email")||"").trim().toLowerCase(),source=String(formData.get("source")||""),product=String(formData.get("product")||""),requirement=String(formData.get("requirement")||"").trim();if(!email.includes("@"))throw new Error("邮箱格式不正确");if(!requirement)throw new Error("客户需求不能为空");const duplicate=row<{id:number}>("SELECT id FROM customers WHERE primary_email=? COLLATE NOCASE AND id<>?",email,current.customer_id);if(duplicate)throw new Error("该邮箱已属于另一个客户，请先进行客户合并或使用关联邮箱");db.prepare("UPDATE customers SET primary_email=?,updated_at=datetime('now','localtime') WHERE id=?").run(email,current.customer_id);db.prepare("UPDATE inquiries SET source=?,product_category=?,requirement=?,updated_at=datetime('now','localtime') WHERE id=?").run(source,product,requirement,id);audit(s.id,"UPDATE","INQUIRY",id,"修改询盘核心资料",{before:current,after:{primary_email:email,source,product_category:product,requirement}});revalidatePath(`/inquiries/${id}`);revalidatePath("/inquiries");revalidatePath("/dashboard")}
export async function updateInquiry(formData:FormData){const id=Number(formData.get("id")),s=await canAccess(id);const before=row<any>("SELECT status,next_follow_up_at,deal_amount FROM inquiries WHERE id=?",id);const status=String(formData.get("status")),next=String(formData.get("next")||"")||null,amount=Number(formData.get("amount")||0)||null;db.prepare("UPDATE inquiries SET status=?,next_follow_up_at=?,deal_amount=?,updated_at=datetime('now','localtime') WHERE id=?").run(status,next,amount,id);audit(s.id,"UPDATE","INQUIRY",id,"更新询盘状态",{before,after:{status,next,amount}});revalidatePath(`/inquiries/${id}`);revalidatePath("/dashboard")}
export async function addFollowUp(formData:FormData){const id=Number(formData.get("id")),s=await canAccess(id),method=String(formData.get("method")),content=String(formData.get("content")||"").trim(),reply=String(formData.get("reply")||"").trim(),next=String(formData.get("next")||"")||null;if(!content)throw new Error("请填写沟通摘要");db.prepare("INSERT INTO follow_ups(inquiry_id,user_id,method,content,customer_reply,next_follow_up_at) VALUES(?,?,?,?,?,?)").run(id,s.id,method,content,reply||null,next);db.prepare("UPDATE inquiries SET status=CASE WHEN status='NEW' THEN 'CONTACTING' ELSE status END,next_follow_up_at=?,updated_at=datetime('now','localtime') WHERE id=?").run(next,id);audit(s.id,"FOLLOW_UP","INQUIRY",id,`新增${method}跟进记录`);revalidatePath(`/inquiries/${id}`);revalidatePath("/dashboard")}
export async function updateCustomer(formData:FormData){const id=Number(formData.get("inquiryId")),s=await canAccess(id),linked=row<{customer_id:number}>("SELECT customer_id FROM inquiries WHERE id=?",id);if(!linked)throw new Error("询盘不存在");const customerId=linked.customer_id;const before=row<any>("SELECT * FROM customers WHERE id=?",customerId);const values=["name","company","country","phone","alternate_emails","note"].map(k=>String(formData.get(k)||"").trim()||null);db.prepare("UPDATE customers SET name=?,company=?,country=?,phone=?,alternate_emails=?,note=?,updated_at=datetime('now','localtime') WHERE id=?").run(...values,customerId);audit(s.id,"UPDATE","CUSTOMER",customerId,"修改客户资料",{before,after:Object.fromEntries(["name","company","country","phone","alternate_emails","note"].map((k,i)=>[k,values[i]]))});revalidatePath(`/inquiries/${id}`)}
export async function syncInquiryToCrm(formData:FormData){const id=Number(formData.get("id")),s=await canAccess(id);const q=row<any>("SELECT i.*,c.primary_email,c.name contact_name,c.company,c.country,c.phone,m.crm_username,m.crm_password,a.value base_url,cs.crm_customer_id,cs.crm_contact_created FROM inquiries i JOIN customers c ON c.id=i.customer_id LEFT JOIN crm_user_mappings m ON m.user_id=i.owner_id LEFT JOIN app_settings a ON a.key='crm_base_url' LEFT JOIN crm_syncs cs ON cs.inquiry_id=i.id WHERE i.id=?",id);if(!q?.owner_id){return}if(q.crm_contact_created){return}let errorMessage="";let crmCustomerId=q.crm_customer_id||null;try{if(!q.base_url||!q.crm_username||!q.crm_password)throw new Error("负责人尚未配置悟空CRM账号");const result=await syncToWukong({base:q.base_url,username:q.crm_username,password:decryptSecret(q.crm_password),customerId:crmCustomerId,name:q.company||q.contact_name||q.primary_email,contactName:q.contact_name||q.primary_email,email:q.primary_email,phone:q.phone||"",country:q.country||"",source:q.source,product:q.product_category,requirement:q.requirement,inquiryNo:q.inquiry_no});crmCustomerId=result.customerId;db.prepare("INSERT INTO crm_syncs(inquiry_id,crm_customer_id,crm_contact_created,synced_by,synced_at,last_error) VALUES(?,?,1,?,datetime('now','localtime'),NULL) ON CONFLICT(inquiry_id) DO UPDATE SET crm_customer_id=excluded.crm_customer_id,crm_contact_created=1,synced_by=excluded.synced_by,synced_at=excluded.synced_at,last_error=NULL").run(id,crmCustomerId,s.id);audit(s.id,"CRM_SYNC","INQUIRY",id,`同步到悟空CRM客户 #${crmCustomerId}`)}catch(error){crmCustomerId=(error as any)?.customerId||crmCustomerId;errorMessage=error instanceof Error?error.message:"同步失败";db.prepare("INSERT INTO crm_syncs(inquiry_id,crm_customer_id,crm_contact_created,synced_by,last_error) VALUES(?,?,0,?,?) ON CONFLICT(inquiry_id) DO UPDATE SET crm_customer_id=excluded.crm_customer_id,synced_by=excluded.synced_by,last_error=excluded.last_error").run(id,crmCustomerId,s.id,errorMessage);audit(s.id,"CRM_SYNC_FAILED","INQUIRY",id,`悟空CRM同步失败：${errorMessage}`)}revalidatePath(`/inquiries/${id}`)}
