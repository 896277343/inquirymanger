import { requireSession } from "@/lib/auth";
import { rows } from "@/lib/db";
import { changeOwnPassword,resetUserPassword } from "./actions";

export default async function PasswordSettings(){const s=await requireSession();const users=s.role==="LEADER"?rows<any>("SELECT id,username,display_name,role FROM users WHERE active=1 ORDER BY CASE role WHEN 'LEADER' THEN 0 ELSE 1 END,id"):[];return <><div className="topline"><div><div className="title">账号与密码</div><p className="subtle">密码采用不可逆哈希保存，系统不会显示当前密码</p></div></div><section className="panel"><strong>修改自己的密码</strong><form action={changeOwnPassword} style={{maxWidth:520}}><label className="field">当前密码<input name="oldPassword" type="password" required autoComplete="current-password"/></label><label className="field">新密码<input name="newPassword" type="password" minLength={6} required autoComplete="new-password"/></label><label className="field">再次输入新密码<input name="confirmPassword" type="password" minLength={6} required autoComplete="new-password"/></label><button className="btn btn-primary">修改我的密码</button></form></section>{s.role==="LEADER"&&<section className="panel"><div className="toolbar"><strong>管理员重置账号密码</strong><span className="subtle">无需知道对方旧密码</span></div><table className="table"><thead><tr><th>姓名</th><th>登录账号</th><th>角色</th><th>设置新密码</th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td><strong>{u.display_name}</strong></td><td>{u.username}</td><td>{u.role==="LEADER"?"领导":"业务员"}</td><td><form action={resetUserPassword} className="actions"><input type="hidden" name="userId" value={u.id}/><input name="newPassword" type="password" minLength={6} required placeholder="至少6位"/><input name="confirmPassword" type="password" minLength={6} required placeholder="再次输入"/><button className="btn">重置密码</button></form></td></tr>)}</tbody></table></section>}</>}
