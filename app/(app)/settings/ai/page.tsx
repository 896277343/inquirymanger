import { requireLeader } from "@/lib/auth";
import { row } from "@/lib/db";
import { saveAiSettings } from "./actions";

const setting=(key:string,fallback:string)=>row<{value:string}>("SELECT value FROM app_settings WHERE key=?",key)?.value||fallback;
export default async function AiSettings(){await requireLeader();const protocol=setting("ai_protocol","RESPONSES"),endpoint=setting("ai_endpoint","https://api.openai.com/v1/responses"),model=setting("ai_model","gpt-5.5"),hasKey=Boolean(row("SELECT value FROM app_settings WHERE key='ai_api_key'"));return <><div className="topline"><div><div className="title">AI接口配置</div><p className="subtle">仅领导可查看；API Key 加密保存在本机数据库中</p></div></div><form action={saveAiSettings} className="panel"><label className="field">接口协议<select name="protocol" defaultValue={protocol}><option value="RESPONSES">Responses API（推荐）</option><option value="CHAT_COMPLETIONS">Chat Completions 兼容协议</option></select></label><label className="field">完整请求地址<input name="endpoint" defaultValue={endpoint} placeholder="https://api.example.com/v1/responses" required/></label><label className="field">模型名称<input name="model" defaultValue={model} placeholder="gpt-5.5" required/></label><label className="field">API Key<input name="apiKey" type="password" placeholder={hasKey?"已配置；留空表示不修改":"sk-..."}/></label><div className="panel" style={{background:"#f7f9fc"}}><strong>兼容说明</strong><p className="subtle">Responses 模式通常使用以 /v1/responses 结尾的地址；通用中转接口若只支持 OpenAI Chat 格式，请选择 Chat Completions 并填写以 /v1/chat/completions 结尾的地址。</p></div><button className="btn btn-primary">保存配置</button></form></>}
